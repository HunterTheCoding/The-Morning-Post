
// import useAxiosPublic from "../../../Hook/useAxiosPublic";
// import Context from "../../../Hook/useContext";


const BooksMarks = () => {
    // const axiosPublic = useAxiosPublic()
    // const {user} = Context()
    // axiosPublic.get(`/donation/${user.email}`)
    return (
        <div>
            <h1>bookmarks</h1>
            
        </div>
    );
};

export default BooksMarks;